# 企业微信会话内容存档看板 — 交付概览

> 生成日期：2026-08-10 | 专家团：MVP开发专家团 v2.1.0（大湾区靓仔/许清楚/高见远/颜好看 等）
> 阶段：Phase 1 完成（三文档产出 + 一致性检查），等待用户确认

## 做了什么

基于用户提供的 `方案.md`（2026-08-08 调研），专家团并行产出三份正式文档，目标是把"公司所有企业微信群聊天记录拉出来并网页展示"的需求结构化：

1. **PRD.md**（主交付，许清楚）— 问题陈述、用户画像、竞品分析（4 直接竞品 + 3 替代）、差异化、RICE 功能排序、MVP 范围、验收标准、边界、非功能需求、数据埋点。
2. **架构设计.md**（支撑，高见远）— 技术选型矩阵 + 7 条 MADR 格式 ADR、分层架构图、数据流、10 个 API 端点草稿、可执行 SQL Schema（4 表）、5 天数据窗口策略、RSA/AES 解密流程、部署与密钥管理、成本、风险、12 条 pitfalls、Lucide 图标锁定。
3. **UIUX设计.md + design-tokens.json**（支撑，颜好看）— 企业微信原生风设计系统 Token、IA 5 页、页面线框、11 类消息渲染规范、Lucide 图标清单、加载/空/错误/极端态。

## 关键决策（已锁定）

- **技术路线：会话内容存档（Chat Audit），不是群机器人/Webhook**。群机器人只能推消息、智能机器人只能收 @消息，均无法拉全量聊天记录。唯一官方途径是后端 SDK 定时拉取 + RSA/AES 解密。
- **权限现状：尚未购买**（约 900 元/人/年，买 1 人覆盖其全部群），PRD 已将其列为前置依赖而非 MVP 功能。
- **MVP 三核心**：① 会话拉取+解密+存储 ② 网页会话展示 ③ 全文检索与筛选（RICE 3.6–4.5）。
- **视觉风格：企业微信原生风**（左群列 + 右聊天时间线，左白右绿气泡，外部联系人带身份标签）。
- **图标库：Lucide**（架构师+设计师一致锁定，16/20/24px 描边，全站不混用）。

## 待用户确认/修正的 3 处（Phase 1 交互点）— 已确认并采纳

1. 导出端点：`/export` 在架构清单里但 PRD 归 Backlog → **已修正**：架构 `/export` 标注"后续/Backlog，MVP 不实现"。
2. 成本：PRD"约1500元" vs 架构"1600–2300元" → **已修正**：PRD 统一为"约 1600–2300 元/首年"。
3. 消息类型：PRD"28种全渲染" → **已修正**：MVP 优先常用类型（文本/图片/语音/视频/文件/链接/撤回），其余走通用卡片兜底。

## 下一步

- 用户已确认三文档并采纳建议修正 → PRD 定稿（本交付范围已完成）。
- 如需继续：进入 Phase 1.5 生成 Spec（openapi.yaml 等）→ Phase 2 设计细化 → Phase 3 并行开发 → Phase 4 测试交付。
