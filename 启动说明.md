# 启动说明（MVP 演示态）

企业微信会话内容存档看板 —— 可运行的最小可用版本（MVP）。

## 1. 环境要求
- **Python 3.13**（后端，已验证）
- **Node.js 22**（前端，已验证）
- 演示态**无需**企业微信权限、无需 Docker、无需 Linux SDK —— 用 SQLite + Mock 拉取器注入种子数据，开箱即看效果。

## 2. 后端启动
```bash
cd backend

# 1) 建虚拟环境并装依赖
python -m venv .venv
.venv\Scripts\pip install -r requirements.txt     # Windows
# .venv/bin/pip install -r requirements.txt        # Linux / macOS

# 2) 启动（演示态默认 PULLER=mock，自动注入种子）
.venv\Scripts\python -m uvicorn app.main:app --port 8000
```
- 健康检查：`http://localhost:8000/api/v1/health`
- 接口文档（Swagger）：`http://localhost:8000/docs`

> 依赖说明：`requirements.txt` 已固定 `bcrypt==4.0.1`。`passlib 1.7.4` 与 `bcrypt>=4.1` 不兼容（无法读取版本号会导致登录校验告警），请勿改成 4.1+。

## 3. 前端启动
```bash
cd frontend

npm install      # 首次或依赖变更时执行；已安装可跳过
npm run dev      # 默认 5173，Vite 自动把 /api 代理到后端 :8000
```
- 访问：`http://localhost:5173`
- 若 5173 被占用，Vite 会自动顺延到 5174/5175…；也可 `npm run dev -- --port 5180`。

## 4. 演示登录
- 账号：`admin`
- 密码：`admin123`

登录后即可看到三类会话：外部客户群、内部群、单聊；点开任意会话查看消息时间线，顶部搜索框支持关键词 + 群 / 发送者 / 类型 / 时间组合检索。

## 5. 演示态 vs 生产态
| 维度 | 演示态（默认） | 生产态 |
|------|---------------|--------|
| 拉取器 | `PULLER=mock`（种子数据） | `PULLER=wecom`（真实 SDK 解密） |
| 数据库 | SQLite（本地文件） | PostgreSQL |
| 企业微信权限 | 不需要 | 需购买「会话内容存档」许可（约 900 元/人/年）、上传 RSA 公钥、配置员工范围 |
| 运行环境 | Windows / macOS 直接跑 | 需 Docker(Linux) + 官方 C SDK v3.0 + RSA 私钥（环境变量注入，绝不入仓库） |

生产态切换步骤、解密链路（RSA PKCS1 v1.5 + AES）、5 天数据留存窗口策略、12 个落地坑，详见 **架构设计.md** 与 **Spec.md**。

## 6. 端口冲突处理
- 后端端口：`uvicorn ... --port <新端口>`；前端代理目标在 `frontend/vite.config.ts`（`target: 'http://localhost:8000'`），端口改了同步改这里即可。
- 全文检索：演示态为 JSON 文本模糊匹配；生产态建议 PostgreSQL `tsvector` / GIN 索引（已在架构设计标注）。

## 7. 已实现 / 未实现（PRD 边界）
- ✅ MVP 三件套：会话拉取解密存储（演示用 Mock）、网页展示、全文检索筛选
- ⏸️ 暂未做（Backlog）：导出、分级权限、敏感词告警、离职继承、消息 `/export` 端点
