# 企业微信会话内容存档看板 — 产品需求文档（PRD）

> 文档版本：v1.0
> 产品负责人：许清楚（产品经理）
> 编制日期：2026-08-10
> 文档定位：本 PRD 是 MVP 研发的输入基线，配合《架构方案》与《UIUX 设计稿》共同交付。本次仅产出文档，不进入开发。
> 竞品调研与差异化结论基于联网实测（2026-08-10）。

---

## 0. 阅读约定

- 本文档中的"图标"均用文字描述（如"搜索图标""导出图标"），不预置任何图形。
- 图标方案：全站统一采用 **SVG 图标库**（具体选型由架构师在 Spec 中锁定，PRD 阶段不预设具体库）。禁止使用 emoji 作为功能图标。
- 视觉风格目标：企业微信原生风（中性、克制、信息密度高）。具体视觉规范由设计师在 UIUX 稿中定义，本文档只标注方向。
- 禁止事项（P0）：禁止 emoji 功能图标、禁止紫色→粉色渐变、禁止 AI 模板味文案（不出现 Welcome to / Lorem ipsum / 硬编码颜色值除 #fff #000 / 弹跳缓动）。

---

## 1. 问题陈述

### 1.1 痛点场景
公司通过大量企业微信群（客户群、外部群）与客户沟通。一旦需要回顾、检索、管理这些聊天记录，团队面临：

- **查不到**：企业微信原生不提供"导出全部聊天记录"能力；云端仅短期留存（内部消息约 90 天、外部消息约 7 天），且非全部群可一键查看。业务人员想回看某个客户群的历史对话，只能在本机客户端手动翻页。
- **搜不出**：已存档的内容散落在多个群、多个员工账号下，无统一全文检索与按群/发送者/时间/类型的筛选手段，关键表述（如"报价""承诺""投诉"）难以在数百万条记录中快速定位。
- **管不住**：无精细化的访问权限分级，任意管理员可查看全部客户对话；无留痕审计，敏感信息存在外泄隐患；员工离职后对话资产交接无依据。
- **留不住（历史盲区）**：会话内容存档功能开通前的历史消息官方不回溯，合规审计最想追溯的"过去"恰恰拿不到（来源：CSDN 合规回溯案例，2026）。

### 1.2 现状解决方案及其不足
| 现有方式 | 做法 | 为什么不行 |
|----------|------|------------|
| 企业微信原生存档 | 购买会话存档权限后由管理员后台查看 | 无检索、无分级权限、业务人员需走审批流程，体验差；历史数据不回溯 |
| 手工截图/转发/手机端导出 | 群成员手动复制 | 量大不可行、易遗漏、时间线错乱、各成员视角不一致 |
| 第三方 SCRM（微伴/尘锋/探马/微盛） | 采购一体化私域运营套件 | 功能臃肿、学习成本高、按人/按套件收费贵（年费数千至数十万元），对小团队过重；很多高级分析需升级付费 |

### 1.3 根因
公司真正需要的不是"再买一套 SCRM"，而是**一个轻量、聚焦、可自托管、便宜的工具**：把官方会话存档 SDK 拉取到的全量记录，统一查看、检索、管理。这正是本产品的定位。

---

## 2. 目标用户

### 2.1 主要用户画像
| 画像 | 角色 | 场景 | 技术水平 | 核心诉求 |
|------|------|------|----------|----------|
| 运营/销售主管 | 30-45 岁，一线业务管理者 | 复盘客户群沟通、定位报价与承诺、交接离职员工对话 | 中低（会用企微/网页） | 快速找到某群某人的某句话，确认服务是否到位 |
| 合规/风控专员 | 28-45 岁，金融/医美等强监管行业 | 定期审计敏感话术、留痕举证 | 中 | 可控的分级查看、操作留痕、可导出审计台账 |

### 2.2 次要用户画像
| 画像 | 角色 | 场景 | 核心诉求 |
|------|------|------|----------|
| 管理者/老板 | 企业决策者 | 担心飞单、客户资产流失、纠纷无据 | 低成本掌控全集沟通留痕 |
| 普通员工（被存档方） | 销售/客服 | 在客户端看到"会话存档"提示，知情存档 | 流程透明、不增加额外操作负担 |

---

## 3. 竞品分析

### 3.1 直接竞品（SCRM / 会话存档可视化工具）
| 竞品 | 核心功能 | 优势 | 劣势（来自差评/实测） | 定价 |
|------|----------|------|----------------------|------|
| 微伴助手 | 永久会话存档、智能检索、敏感词预警、离职继承 | 头部 ISV，腾讯投资；永久存储、撤回/删除消息可找回；AI 多维度检索 | 基础版数百元/月，团队用成本高；部分高级功能（旅程自动化、报表）需升版本；初始上手有学习成本 | 按套件，基础版数百元/月起步 |
| 尘锋 SCRM | 会话存档、聊天监控、销售全链路、工商搜客 | 获客+跟单+管理全链路；风控质检成熟 | 功能重、配置复杂，新手学不会（实测"学三天仍不会配自动化"）；客服响应慢 | 定制报价，标准版约 840-1200 元/用户/年 |
| 探马 SCRM | 会话存档、会话质检、营销素材分析、工单协同 | 销售+私域一体化；会话质检较细 | 年费门槛高（实测称可达 10 万元）；重运营场景，轻量查看需求被淹没 | 定制，年费门槛高 |
| 微盛·企微管家 | 客户运营+营销获客+SCRM+会话存档 四大能力 | 腾讯四轮投资，规模大；人均成本低（约 330 元/年）；AI 打标 | 高级版/AI 版刊例价极高（高级版 28 万/年、AI 版 48 万/年）；重私域运营，轻量查看非重点 | 标准版 7000 元/年；会话存档模块另计 |

### 3.2 替代方案
| 方案 | 说明 | 劣势 |
|------|------|------|
| 企业微信原生存档 + 管理员后台 | 官方能力，开通后由管理员在后台查看 | 无全文检索、无分级权限、业务人员需审批；**历史数据不回溯**；外部消息云端仅约 7 天 |
| 人工导出（截图/转发/手机端导出） | 群成员手动复制 | 量大不可行、易遗漏、时间线错乱、各视角不一致 |
| RPA 模拟翻看（社区偏方） | 程序模拟人在客户端"翻看+复制" | 非官方、有合规与稳定性风险；仅能补历史盲区，不能替代实时合规存档 |

### 3.3 差评揭示的真实痛点（市场空白）
1. **贵且重**：一体化 SCRM 年费数千至数十万，小团队为"查看记录"付出过多。
2. **学不会**：功能堆叠导致上手成本高，轻量查看诉求被复杂系统淹没。
3. **历史盲区**：开通前的消息官方不回溯，合规最需要的"过去"拿不到（本产品明确为已知边界，不在 MVP 解决，见边界条件）。
4. **原生能力弱**：官方后台无检索、无分级权限、无留痕，业务人员使用门槛高。

---

## 4. 我们的差异化

- **聚焦而非堆功能**：只做"拉取 → 查看 → 检索 → 管理"一条线，不做营销自动化、裂变、SOP 等重运营模块，降低学习成本。
- **便宜且可自托管**：官方会话存档权限（服务版约 900 元/人/年，买 1 人即可覆盖其参与的全部群）+ 自有 Linux 服务器，首年成本约 1600–2300 元，远低于 SCRM 套件。
- **企业微信原生风**：界面贴合企微视觉与交互习惯，内部员工零学习成本。
- **检索优先**：以"快速定位关键对话"为第一设计目标（全文检索 + 多维筛选），直击"搜不出"痛点。
- **合规克制**：分级权限 + 操作留痕作为演进方向，满足强监管行业对"管不住"的顾虑（MVP 先不做，见 Backlog）。

---

## 5. 核心功能（RICE 排序）

> RICE = (Reach × Impact × Confidence) / Effort
> Reach 1-10（每季度受影响用户数）；Impact 0.25/0.5/1/2/3；Confidence 50%/80%/100%；Effort 1-10（人月）

| 功能 | Reach | Impact | Confidence | Effort | Score | MVP? |
|------|-------|--------|------------|--------|-------|------|
| 会话拉取+解密+存储（后端定时拉取 SDK、RSA/AES 解密、结构化入库） | 10 | 3 | 100% | 8 | 3.75 | ✅ |
| 网页会话展示（群聊列表 + 时间线聊天视图 + 多消息类型渲染） | 9 | 3 | 100% | 6 | 4.50 | ✅ |
| 全文检索与筛选（关键词 + 群/发送者/类型/时间范围） | 8 | 2 | 90% | 4 | 3.60 | ✅ |
| 导出（CSV / PDF 审计台账） | 5 | 1 | 90% | 3 | 1.50 |  backlog |
| 分级权限管控（RBAC + 操作留痕） | 6 | 1 | 80% | 3 | 1.60 |  backlog |
| 敏感词预警/风控质检 | 4 | 2 | 70% | 5 | 1.12 |  backlog |
| 离职客户对话继承 | 3 | 1 | 70% | 4 | 0.52 |  backlog |

**RICE 结论**：前三项（网页会话展示 4.50、会话拉取解密存储 3.75、全文检索与筛选 3.60）为最高优先级，构成 MVP 核心。其中"会话拉取+解密+存储"是地基能力，无它则其余功能不成立。

---

## 6. MVP 范围

### 6.1 纳入 MVP 的三个核心功能
1. **会话拉取 + 解密 + 存储**：后端定时（1-5 分钟增量）调用官方会话存档 SDK，RSA 解密 `encrypt_random_key`、AES 解密 `encrypt_chat_msg`，结构化写入数据库（群信息、消息记录、成员映射三张核心表）。
2. **网页会话展示**：左侧群聊列表（群名、消息数、最后消息时间、搜索图标）+ 右侧时间线聊天视图（仿企微聊天气泡）。MVP 优先打磨常用类型：文本/图片（缩略图+点击放大图标）/语音（播放器+时长）/视频（播放器）/文件（下载图标+大小）/链接·小程序卡片/撤回系统条；其余 28 种消息类型（名片/红包/位置/表情等）走统一通用卡片兜底，后续迭代细化。
3. **全文检索与筛选**：顶部搜索框（搜索图标）按消息正文检索；筛选栏支持按群、发送者、消息类型、时间范围组合过滤。

### 6.2 前置依赖（非 MVP 功能，必须写清但不计入研发）
> 这些是"能跑通 MVP"的前置条件，由运维/管理员在管理后台完成，不计入 MVP 研发工作量：
1. 购买会话存档权限（服务版约 900 元/人/年，买 1 人即覆盖其参与的所有群）。
2. 企业微信管理后台配置存档员工范围（覆盖参与客户群沟通的员工）。
3. 生成 RSA 密钥对（`openssl genrsa -out private_key.pem 2048` + 导出公钥），在后台上传公钥。
4. 记录 corpid 与会话存档 Secret，私钥安全存储（环境变量/密钥管理服务，不入代码仓库）。

### 6.3 明确不做（MVP 之外，进 Backlog）
导出、分级权限与操作留痕、敏感词预警、离职继承、AI 分析、营销自动化、移动端原生 App。

---

## 7. 验收标准（Given / When / Then）

**AC-1 会话拉取解密存储**
- Given 已完成前置依赖（权限已购、员工范围已设、RSA 公钥已上传、私钥已安全配置），When 后端定时任务运行，Then 在 5 天数据窗口内、被存档员工参与的群聊与单聊消息被增量拉取、解密并结构化入库，撤回消息以 `action=recall` 标记保留。

**AC-2 网页群聊列表**
- Given 数据库已有存档消息，When 用户登录网页并打开左侧群聊列表，Then 按群分组展示群名、消息数、最后消息时间，并可通过搜索图标按群名过滤。

**AC-3 时间线聊天视图**
- Given 用户选中某群，When 页面加载，Then 按时间正序渲染消息气泡，正确渲染至少文本/图片/语音/视频/文件/链接六类消息，且图片可点击放大、语音/视频可播放、文件可下载。

**AC-4 全文检索**
- Given 用户进入检索，When 输入关键词并提交，Then 返回命中消息及其所属群、发送者、时间，点击结果可跳转至对应对话位置。

**AC-5 多维筛选**
- Given 用户使用筛选栏，When 按"群 + 发送者 + 消息类型 + 时间范围"任意组合筛选，Then 时间线仅展示符合条件的消息，且筛选条件可清空复位。

**AC-6 解密正确性**
- Given 一条加密消息，When 系统完成 RSA+AES 解密，Then 明文内容与官方客户端显示一致（以消息 ID 对齐校验），无乱码、无丢失字段。

---

## 8. 边界条件

- **空状态**：无存档数据时，群聊列表与聊天视图展示空状态引导（提示完成前置依赖配置），不报错。
- **错误状态**：SDK 拉取失败 / 私钥不匹配 / 解密异常时，记录错误日志并展示可读错误提示（如"解密失败，请检查私钥配置"），不白屏。
- **加载状态**：长列表与媒体加载显示加载态（如骨架屏/加载图标），列表分页加载。
- **边界值**：单次拉取上限 1000 条，超过以 cursor 分页增量；频率限制 4000 次/分钟需限速；消息时间跨时区以服务器时间戳为准。
- **数据窗口**：官方仅保留约 5 天数据，定时任务中断超过窗口将导致该段数据永久丢失——需监控告警（架构侧实现）。
- **历史盲区**：开通会话存档前的历史消息官方不回溯，本产品无法获取，属已知边界，不在 MVP 范围，需在界面提示。
- **合规边界**：员工与客户需知情同意；客户拒绝存档时仅保留员工侧会话，界面需如实呈现"未授权"状态。
- **并发**：多用户同时检索/查看，后端需限流与连接池保护。
- **离线**：前端离线时展示离线提示，核心查看依赖服务端数据，不承诺离线可用。
- **权限拒绝**：未授权用户访问受控数据，返回无权限提示并留痕。

---

## 9. 非功能需求

| 类别 | 要求 | 优先级 |
|------|------|--------|
| 性能 | 群聊列表首屏 < 3s；消息列表/检索 API p95 < 500ms；万级消息时间线滚动流畅 | P0 |
| 可用性 | 拉取任务无单点故障；解密/存储失败可重试与告警；核心查看流程在部分服务降级时仍可用 | P1 |
| 安全 | HTTPS 全链路 + JWT 鉴权 + 输入校验 + API 速率限制；RSA 私钥不入代码仓库，用密钥管理服务；敏感操作留痕（演进） | P0 |
| 兼容性 | Chrome / Safari / Firefox 最新 2 个版本；企业微信内置浏览器最新版 | P0 |
| 可访问性 | 键盘可达、对比度满足基本可读（参考 WCAG 2.1 AA 基础项） | P2 |
| 国际化 | 当前仅中文；预留 i18n 接口，不强制实现 | P2 |
| 图标方案 | 全站统一 **SVG 图标库**（具体库由架构师在 Spec 锁定）；功能图标以文字描述为准，禁止 emoji 图标 | P0 |
| 视觉风格 | 企业微信原生风：中性色板、克制、信息密度高；禁止紫色→粉色渐变 | P0 |

---

## 10. 数据埋点方案

> MVP 必须埋点，否则上线后无法验证"用户是否真的用来看/搜记录"这一核心假设。

| 事件类别 | 必埋事件 | 说明 |
|----------|----------|------|
| 获客 | `page_view`、`login_complete` | 用户从哪来、登录转化 |
| 激活 | `first_chat_viewed`、`first_search_done` | 用户完成第一个核心动作（首次打开群聊 / 首次检索） |
| 留存 | `session_start`、`session_duration` | DAU/MAU、使用频次 |
| 转化 | `archive_config_done`（前置依赖完成） | 是否走通权限购买与配置闭环 |
| 异常 | `error_occurred`、`decrypt_failed` | 前端错误 + API 错误 + 解密失败专项 |

### 埋点实现要求
- 前端用轻量 SDK（Mixpanel / Umami / 自建 `trackEvent()` 封装），后端关键事件（拉取量、解密失败）走服务端日志。
- 事件命名规范：`{对象}_{动作}`（如 `chat_opened`、`search_submitted`）。
- 属性规范：每个事件附带 `user_id`、`timestamp`、`device`、`version`。
- 不采集隐私数据：不上报 IP、不存储消息原文内容；仅记录聚合计数与脱敏元数据。

---

## 11. 依赖与风险（补充）

| 风险 | 对策 |
|------|------|
| 5 天数据窗口，拉取中断丢数据 | 定时任务 1-5 分钟增量 + 监控告警（架构实现） |
| macOS 不支持原生 SDK | 后端用 Docker（Linux 容器）运行 |
| 客户不同意存档 | 界面如实呈现"未授权"，做好客户告知 |
| 私钥泄露 | 密钥管理服务存储，不入仓 |
| 消息量大数据库压力 | 按 roomid 索引 + 归档策略（架构演进） |

---

## 附：竞品调研来源（2026-08-10 联网检索）
- 微伴助手官网与实测榜（weibanzhushou.com）
- 尘锋 / 探马对比（36dianping.com、xtools.cn）
- 微盛·企微管家定价与能力（36dianping.com、aiqicha.baidu.com、腾讯云市场）
- 企业微信原生存档限制与 2026 调价（wescrm.com、saasruanjian.com）
- 会话存档"历史盲区"与 RPA 偏方（CSDN 案例，2026）
- 技术路线基线：《企业微信聊天记录获取方案》（方案.md，2026-08-08）
